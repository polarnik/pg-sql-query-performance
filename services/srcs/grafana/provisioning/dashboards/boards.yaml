# # config file version
apiVersion: 1

providers:
 - name: 'gatling'
   orgId: 1
   folder: 'gatling'
   type: file
   allowUiUpdates: false
   updateIntervalSeconds: 10
   options:
     path: /etc/grafana/provisioning/dashboards/gatling

 - name: 'jmeter'
   orgId: 1
   folder: 'jmeter'
   type: file
   allowUiUpdates: false
   updateIntervalSeconds: 10
   options:
     path: /etc/grafana/provisioning/dashboards/jmeter

 - name: 'jolokia'
   orgId: 1
   folder: 'jolokia'
   type: file
   allowUiUpdates: false
   updateIntervalSeconds: 10
   options:
     path: /etc/grafana/provisioning/dashboards/jolokia

 - name: 'jvm-profiling'
   orgId: 1
   folder: 'jvm-profiling'
   type: file
   allowUiUpdates: false
   updateIntervalSeconds: 10
   options:
     path: /etc/grafana/provisioning/dashboards/jvm-profiling

 - name: 'kubernetes'
   orgId: 1
   folder: 'kubernetes'
   type: file
   allowUiUpdates: false
   updateIntervalSeconds: 10
   options:
     path: /etc/grafana/provisioning/dashboards/kubernetes